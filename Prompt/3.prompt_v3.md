You are a senior full-stack engineer.

I already have a working CRM dashboard with:

* React (Vite + Tailwind)
* Node.js (Express)
* PostgreSQL
* Socket.io already integrated

Now I want to UPGRADE the dashboard to FULL REAL-TIME behavior.

IMPORTANT:

* Do NOT rebuild anything
* Extend existing Socket.io implementation
* Do not break existing functionality

========================
🎯 OBJECTIVE
============

Make dashboard KPIs, charts, tasks, and activity update instantly without page refresh.

========================
🔌 1. BACKEND REAL-TIME EVENTS
==============================

Modify backend services to emit Socket.io events on:

* New lead created → emit "lead:new"
* Lead status updated → emit "lead:update"
* New deal created → emit "deal:new"
* Deal updated → emit "deal:update"
* New ticket created → emit "ticket:new"
* Ticket updated → emit "ticket:update"
* Task created/updated → emit "task:update"
* Log entry created → emit "activity:new"

Emit to:

* Specific user room (userId)
* Or broadcast if needed

========================
🧠 2. SOCKET STRUCTURE
======================

* Use existing JWT-authenticated socket connection
* Join user to room: user:{userId}
* Emit events using io.to(`user:${userId}`)

========================
⚛️ 3. FRONTEND REAL-TIME HANDLING
=================================

In React:

* Extend NotificationContext or create DashboardSocketContext

Listen for:

* "lead:new"
* "deal:update"
* "ticket:new"
* "task:update"
* "activity:new"

On event:

* Update state immediately
* Update KPI values dynamically
* Update charts without reload
* Show toast notification

========================
📊 4. KPI LIVE UPDATE LOGIC
===========================

* When new lead → increment total leads
* When deal updated → update revenue
* When ticket created → increment open tickets
* When task completed → update pending tasks

Ensure calculations are consistent with backend data

========================
📈 5. CHART REAL-TIME UPDATE
============================

* Update chart datasets dynamically
* Avoid full re-render
* Use state update instead of reload

========================
📋 6. ACTIVITY FEED LIVE
========================

* On "activity:new":

  * Add new item at top
  * Keep only last 8 items
  * Animate entry (fade/slide)

========================
🔔 7. TOAST NOTIFICATIONS
=========================

* Show toast for:

  * New ticket
  * New lead
  * Deal update

Use existing UI library or lightweight toast system

========================
⚡ 8. PERFORMANCE SAFETY
=======================

* Debounce frequent updates
* Avoid multiple re-renders
* Use React.memo where needed

========================
🧪 9. TEST SCENARIOS
====================

* Open dashboard in 2 browser tabs
* Create lead in one tab
* Verify second tab updates instantly

========================
📄 OUTPUT FORMAT
================

* Show only modified backend and frontend files
* Explain changes briefly
* Ensure code works without breaking existing features

========================
⚠️ IMPORTANT
============

* Do step-by-step implementation
* Start with backend event emission
* Then frontend listeners
* Then KPI update logic

Now begin with backend event emission.
